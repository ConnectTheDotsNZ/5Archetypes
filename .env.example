# Postgres connection string. Local Docker, or a free hosted instance (Neon, Supabase, Railway) all work.
DATABASE_URL="postgresql://user:password@localhost:5432/five_archetypes?schema=public"

# Auth is stubbed in this scaffold (see src/lib/auth.ts). Wiring a real provider
# (Clerk, Auth0, etc.) is the first Phase 1 task — see docs/CLAUDE_CODE_KICKOFF.md.
# CLERK_SECRET_KEY=
# NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
